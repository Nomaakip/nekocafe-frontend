<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>nekocafe - nomaakip.xyz</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
   <main>
    <img src="/img/logo.png" alt="logo">
        <div class="wrapper">
            <div class="sidebar box">
                <img src="/img/cafe.png" style="height: 120px; image-rendering: pixelated; text-align: center">
                <span>nekocafe but its a custom frontend</span>
                <br><br>
                <a href="/"><button class="button">DON'T Register!</button></a>
                <br><br>
                <a href="/"><button class="button">DON'T log in!</button></a>
            </div>
            <div class="posts">
                <% postsRes.forEach((post) => { %>
                    <%- include('partials/post', { post }) %>
                <% }); %>
                <div class="pages box">
                    <% for (let i = 0; i < pagesRes.total_pages; i++) { %>
                        <a href="/page/<%= i + 1 %>"><%= i + 1 %></a>
                    <% } %>
                </div>
            </div>
            </div>
        </div>
   </main>
</body>
</html>